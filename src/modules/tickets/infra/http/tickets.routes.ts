import { Router } from "express";

const ticketsRouter = Router();

ticketsRouter.post('/', (req, res) => {
  // Replace with TicketController
})

ticketsRouter.put('/:id', (req, res) => {
  // Replace with TicketController
})

ticketsRouter.delete('/:id', (req, res) => {
  // Replace with TicketController
})


export default ticketsRouter;